import styled from '@emotion/styled';

export const Title = styled.h2`
  color: rgb(38, 38, 51);
  font-size: 1.5rem;
  font-weight: 700;
  line-height: 1.75rem;
  letter-spacing: 0;
  text-align: left;
  margin: 0;
`;

export const Content = styled.div`
  width: 100%;
  & > div {
    padding: 0;
  }
`;
