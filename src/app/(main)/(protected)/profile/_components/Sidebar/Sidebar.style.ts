import styled from '@emotion/styled';
import { Divider } from '@mui/material';

export const Container = styled.div`
  background-color: white;
  display: flex;
  flex-direction: column;
  padding: 16px;
  border-radius: 16px;
  cursor: pointer;
`;

export const DeviderLine = styled(Divider)`
  margin: 16px 0;
`;
