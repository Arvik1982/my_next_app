import * as React from 'react';

//FIXME: ЭТО ВРЕМЕННЫЙ КОД, ОН БУДЕТ ПЕРЕРАБОТАН В СООТВЕТСТВИИ С МАКЕТОМ

export default function CalendarLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return <>{children}</>;
}
